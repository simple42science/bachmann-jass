name: jass_engine
description: Regeln, Wertung und Computergegner fuer Schweizer Jass (Schieber und Bieterjass). Reines Dart, ohne Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
